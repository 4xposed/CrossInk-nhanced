add_executable(LibraryPathsTest LibraryPathsTest.cpp)
target_include_directories(LibraryPathsTest PRIVATE ${REPO_ROOT}/src)
add_executable(LibraryNavigationTest LibraryNavigationTest.cpp)
target_include_directories(LibraryNavigationTest PRIVATE ${REPO_ROOT}/src)
add_executable(LibraryCatalogTest LibraryCatalogTest.cpp ${REPO_ROOT}/src/util/LibraryCatalog.cpp)
target_include_directories(LibraryCatalogTest PRIVATE stubs ${REPO_ROOT}/src)
foreach(target LibraryPathsTest LibraryNavigationTest LibraryCatalogTest)
  target_compile_options(${target} PRIVATE -UNDEBUG -Wall -Wextra -Wno-unused-parameter)
  add_test(NAME ${target} COMMAND ${target})
endforeach()
